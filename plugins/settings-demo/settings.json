{
  "fields": [
    {
      "id": "enabled",
      "type": "boolean",
      "label": "启用插件功能",
      "default": true,
      "description": "开关控件（type: boolean / toggle），保存为 true / false"
    },
    {
      "id": "nickname",
      "type": "text",
      "label": "名称",
      "placeholder": "例如：演示机器人",
      "default": "演示机器人",
      "required": true,
      "description": "单行输入框（type: text），支持 required / placeholder"
    },
    {
      "id": "description_notes",
      "type": "textarea",
      "label": "备注",
      "placeholder": "写点备注…",
      "rows": 4,
      "description": "多行文本框（type: textarea），支持 rows 控制行数"
    },
    {
      "id": "api_token",
      "type": "password",
      "label": "API 令牌",
      "placeholder": "留空保持不变",
      "description": "密码输入框（type: password），不会明文显示"
    },
    {
      "id": "interval",
      "type": "number",
      "label": "轮询间隔（秒）",
      "default": 30,
      "min": 1,
      "max": 3600,
      "step": 1,
      "description": "数字输入框（type: number），支持 min / max / step"
    },
    {
      "id": "threshold",
      "type": "slider",
      "label": "触发阈值",
      "default": 50,
      "min": 0,
      "max": 100,
      "step": 5,
      "description": "范围滑动条（type: slider），拖动即可调整"
    },
    {
      "id": "log_level",
      "type": "select",
      "label": "日志级别",
      "default": "info",
      "options": [
        { "label": "调试", "value": "debug" },
        { "label": "信息", "value": "info" },
        { "label": "警告", "value": "warning" },
        { "label": "错误", "value": "error" }
      ],
      "description": "下拉选择（type: select），options 支持 label / value 对象"
    },
    {
      "id": "hooks_tags",
      "type": "select",
      "label": "简化下拉（字符串数组）",
      "default": "beta",
      "options": ["stable", "beta", "nightly"],
      "description": "下拉的 options 也可以是纯字符串数组（label 与 value 相同）"
    },
    {
      "id": "workspace",
      "type": "directory",
      "label": "工作目录",
      "placeholder": "选择或输入一个目录路径",
      "description": "目录选择（type: directory），带“选择”按钮"
    },
    {
      "id": "config_file",
      "type": "file",
      "label": "配置文件",
      "placeholder": "选择或输入一个文件路径",
      "description": "文件选择（type: file），带“选择”按钮"
    },
    {
      "id": "accent_color",
      "type": "color",
      "label": "强调色",
      "default": "#4f8cff",
      "description": "颜色选择器（type: color），保存为 #rrggbb"
    },
    {
      "id": "custom_headers",
      "type": "key-value",
      "label": "自定义请求头",
      "keyLabel": "Header 名称",
      "valueLabel": "Header 值",
      "description": "键值对列表（type: key-value），可增删行"
    }
  ]
}